## wp rewrite
wp rewrite list [--match=<url>] -- list rewrite rules
wp rewrite flush [--hard] [--no-mod-rewrite] -- flush rewrite rules
wp rewrite structure [<structure>] [--category_base=<base>] [--tag_base=<base>] -- get/set permalink structure
wp rewrite status -- show current rewrite state (structure, rules)
