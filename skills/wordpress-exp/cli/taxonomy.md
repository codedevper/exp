## wp taxonomy
wp taxonomy list [--public=<bool>] [--field=<field>] -- list registered taxonomies
wp taxonomy get <taxonomy> [--field=<field>] -- get taxonomy details
wp taxonomy exists <taxonomy> -- check if a taxonomy is registered (exit code)
