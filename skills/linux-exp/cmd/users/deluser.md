## deluser
sudo deluser <username> -- remove user
sudo deluser --remove-home <username> -- remove user and home dir
sudo deluser --remove-all-files <username> -- remove user and all files
sudo deluser --backup <username> -- backup files before removal
sudo deluser --backup-to <dir> <username> -- backup to specific dir
sudo deluser --group <group> -- remove a group
