## wp package
wp package list -- list installed WP-CLI packages
wp package install <git-url> [--version=<version>] -- install a WP-CLI package from GitHub
wp package update -- update all installed WP-CLI packages
wp package uninstall <name> -- uninstall a WP-CLI package
wp package browse -- browse the WP-CLI package index
