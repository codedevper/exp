## df
df -h -- human-readable disk usage
df -T -- show filesystem type
df -i -- show inode usage
df -a -- include pseudo filesystems
df -x <type> -- exclude filesystem type
df --total -- show total
df -l -- local filesystems only
df -B <size> -- scale sizes by size
df --sync -- sync before showing
df -P -- POSIX output format
